@model PagedList.Core.IPagedList<Model.EF.User>;
@using PagedList.Core.Mvc;

@{
    ViewData["Title"] = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

@section header {
    <h1>User List</h1>
}


<div class="row mt-3">
    <div class="col-md-12">
        <div class="card bg-white">
            <div class="card-body mt-3">
                <div class="table-responsive">
                    <table id="usersTable" class="table table-striped table-borderless" style="width:100%">
                        <thead>
                            <tr>
                                <th>#</th>
                                <th>UserName</th>
                                <th>Name</th>
                                <th>Email</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>
                                        <a href="/Admin/User/Edit/@item.Id">Edit</a>
                                        |
                                        <a href="/Admin/User/Delete/@item.Id">Delete</a>
                                    </td>
                                    <td>@item.UserName</td>
                                    <td>@item.Name</td>
                                    <td>@item.Email</td>
                                </tr>
                            }
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>